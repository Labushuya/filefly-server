STORAGE_ROOT=/data
# REQUIRED — the server refuses to start with the default/empty value.
# Generate a strong secret: openssl rand -hex 32
SECRET_KEY=change-me-in-production
JWT_EXPIRE_HOURS=168
MAX_CHUNK_SIZE_MB=10
ALLOWED_ORIGINS=*
